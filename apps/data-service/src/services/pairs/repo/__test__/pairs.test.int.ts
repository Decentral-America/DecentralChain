import { describe, it } from 'vitest';

// Integration test requires a live database — TODO: rewrite with Effect.runPromise
describe('pairs repo integration (stub)', () => {
  it.todo('should return pairs from the database');
});
