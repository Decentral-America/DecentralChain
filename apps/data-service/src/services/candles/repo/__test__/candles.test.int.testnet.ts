import { describe, it } from 'vitest';

// Integration test requires a live testnet database — TODO: rewrite with Effect.runPromise
describe('candles repo testnet integration (stub)', () => {
  it.todo('should return candles from testnet');
});
