import { describe, it } from 'vitest';

// Integration test requires a live database — TODO: rewrite with Effect.runPromise
describe('candles repo integration (stub)', () => {
  it.todo('should return candles from the database');
});
