# cargo-deny configuration — https://embarkstudios.github.io/cargo-deny/
# Run: cargo deny check

# ────────────────────────────────────────────────────────────────────────────
# [advisories] — vulnerability & yanked crate policy
# ────────────────────────────────────────────────────────────────────────────
[advisories]
# Fetch from the official RustSec advisory database (default).
db-urls = ["https://github.com/RustSec/advisory-db"]

# Yanked crates must be treated as errors.
yanked = "deny"

# Unmaintained advisories fail only when the crate is a direct workspace dep.
unmaintained = "workspace"

# Unsound advisories fail only when the crate is a direct workspace dep.
unsound = "workspace"

# Transitive dependency advisories that cannot be fixed by bumping a direct dep.
# Each entry must document why it is acceptable to ignore.
ignore = [
    # RUSTSEC-2025-0111 — tokio-tar parses PAX extended headers incorrectly (file smuggling).
    # tokio-tar is pulled in transitively by testcontainers-modules (dev/test only).
    # Risk: this crate is used exclusively in integration tests to manage Docker containers;
    # it never processes untrusted archives in production. Track upstream fix at:
    # https://github.com/nickelc/tokio-tar/issues — remove once testcontainers-modules upgrades.
    { id = "RUSTSEC-2025-0111", reason = "test-only transitive dep via testcontainers-modules; no production tar parsing" },
]

# ────────────────────────────────────────────────────────────────────────────
# [licenses] — acceptable SPDX licenses
# ────────────────────────────────────────────────────────────────────────────
[licenses]
# All licenses not in this list are denied by default.
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "BSD-3-Clause",
    "Unicode-3.0",
    "Zlib",
]

# Confidence threshold for license text matching.
confidence-threshold = 0.8

# Ignore the license of workspace-private crates (publish = false).
[licenses.private]
ignore = true

# ────────────────────────────────────────────────────────────────────────────
# [bans] — forbidden crates and duplicate version policy
# ────────────────────────────────────────────────────────────────────────────
[bans]
# Warn on duplicate crate versions (common in large Rust dependency trees).
multiple-versions = "warn"

# blake2 0.10.6 requires digest 0.10/block-buffer 0.10/crypto-common 0.1;
# sha3 0.11.0 requires digest 0.11/block-buffer 0.12/crypto-common 0.2.
# These are both at their latest versions — the split is upstream-unavoidable.
skip = [
    { crate = "digest", reason = "blake2 0.10 and sha3 0.11 use different digest API generations" },
    { crate = "block-buffer", reason = "digest 0.10 and 0.11 require different block-buffer versions" },
    { crate = "crypto-common", reason = "digest 0.10 and 0.11 require different crypto-common versions" },
    { crate = "wit-bindgen", reason = "wasip3 and wasmtime-wasi use different wit-bindgen generations" },
]

# Wildcard versions are forbidden in published deps.
wildcards = "deny"

# Banned crates: well-known insecure or deprecated alternatives.
deny = [
    # openssl has a long history of CVEs; rustls or native-tls preferred
    { crate = "openssl", reason = "Use rustls or native-tls instead" },

    # unmaintained alternatives
    { crate = "failure", reason = "Use thiserror/anyhow instead" },
    { crate = "error-chain", reason = "Use thiserror/anyhow instead" },

    # outdated rand version (0.7 and earlier had RNG quality issues)
    "rand:<=0.7",
]

# ────────────────────────────────────────────────────────────────────────────
# [sources] — only allow crates from crates.io
# ────────────────────────────────────────────────────────────────────────────
[sources]
# Deny crates from unknown registries or git sources.
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
