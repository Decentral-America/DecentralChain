// store/types.ts
// The AppAction union and typedPayload helper have been removed.
// Action types are now inferred from createSlice action creators in reducers/*.ts.
// Use `UnknownAction` from @reduxjs/toolkit for generic action type parameters.
