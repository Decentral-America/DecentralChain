# DecentralChain Ecosystem — act secrets template
# Copy to .secrets and fill in real values. NEVER commit .secrets.
#
# Generate a GitHub PAT at: https://github.com/settings/tokens
# Required scopes: repo, read:packages, write:packages

GITHUB_TOKEN=
GHCR_TOKEN=
NX_CLOUD_ACCESS_TOKEN=
CODECOV_TOKEN=
