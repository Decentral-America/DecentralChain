name: BPS CI

on:
  push:
    branches: [main, dev]
    paths:
      - 'apps/blockchain-postgres-sync/**'
  pull_request:
    branches: [main, dev]
    paths:
      - 'apps/blockchain-postgres-sync/**'
  workflow_dispatch:

concurrency:
  group: bps-ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build-and-test:
    name: Build, test & lint
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: stable
          components: rustfmt, clippy, llvm-tools-preview

      - name: Install system dependencies
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            protobuf-compiler \
            libpq-dev

      - name: Install cargo-nextest, cargo-llvm-cov, cargo-deny, cargo-machete
        uses: taiki-e/install-action@6887963ccf37a9ddcd8c5fa4baeb3e1e5fd61fa1 # v2.81.2
        with:
          tool: cargo-nextest,cargo-llvm-cov,cargo-deny@0.19.7,cargo-machete

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: apps/blockchain-postgres-sync
          cache-on-failure: true

      - name: Check formatting
        working-directory: apps/blockchain-postgres-sync
        run: cargo fmt --all -- --check

      - name: Build
        working-directory: apps/blockchain-postgres-sync
        run: cargo build --all-targets

      - name: Clippy (deny warnings)
        working-directory: apps/blockchain-postgres-sync
        run: cargo clippy --all-targets -- -D warnings

      - name: Test with coverage (cargo-nextest + cargo-llvm-cov)
        id: test
        working-directory: apps/blockchain-postgres-sync
        run: |
          set -euo pipefail
          cargo llvm-cov nextest \
            --all-targets \
            --codecov \
            --output-path codecov.json

      - name: Upload coverage to Codecov
        if: steps.test.outcome == 'success'
        uses: codecov/codecov-action@e79a6962e0d4c0c17b229090214935d2e33f8354 # v6.0.1
        with:
          files: apps/blockchain-postgres-sync/codecov.json
          flags: bps
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true

      - name: Check documentation
        working-directory: apps/blockchain-postgres-sync
        run: RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features

      # ── Security & dependency policy ──────────────────────────────────────────
      - name: Dependency policy check (cargo deny)
        working-directory: apps/blockchain-postgres-sync
        run: cargo deny check

      - name: Unused dependency check (cargo machete)
        working-directory: apps/blockchain-postgres-sync
        run: cargo machete
