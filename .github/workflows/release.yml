name: Release

on:
  workflow_dispatch:
    inputs:
      dry-run:
        default: false
        description: Run without publishing
        type: boolean

concurrency:
  cancel-in-progress: false
  group: release

permissions:
  contents: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Guard — enforce main branch
        if: github.ref != 'refs/heads/main'
        run: |
          echo "::error::Release workflow triggered from '${{ github.ref }}'. Only 'refs/heads/main' is allowed."
          exit 1

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup pnpm
        uses: pnpm/action-setup@ab5d408e9d066b8ffe7503f789d50fd7e35c0c92 # v6.0.7

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          cache: pnpm
          node-version-file: .node-version
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Security audit
        run: pnpm audit --audit-level=high

      - name: Biome format
        run: npx biome format .

      - name: Biome lint
        run: npx nx run-many -t biome-lint --projects='packages/ts/*'

      - name: Typecheck
        run: npx nx run-many -t typecheck --projects='packages/ts/*'

      - name: Build
        run: npx nx run-many -t build --projects='packages/ts/*'

      - name: Test
        run: npx nx run-many -t test --projects='packages/ts/*'

      - name: Verify package exports
        run: npx nx run-many -t check:exports --projects='packages/ts/*'

      - name: Verify package publishing
        run: npx nx run-many -t check:publint --projects='packages/ts/*' --exclude=ts-types

      - name: Verify bundle size
        run: npx nx run-many -t check:size --projects='packages/ts/*'

      - name: Version
        run: npx nx release version --skip-publish ${{ inputs.dry-run && '--dry-run' || '' }}

      - name: Changelog
        run: npx nx release changelog --skip-publish ${{ inputs.dry-run && '--dry-run' || '' }}

      - name: Publish
        if: ${{ !inputs.dry-run }}
        run: npx nx release publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Generate SBOM
        if: ${{ !inputs.dry-run }}
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          format: spdx-json
          artifact-name: sbom-${{ github.sha }}.spdx.json
          upload-artifact: true
          upload-release-assets: false
