<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- ── Coordinates ──────────────────────────────────────────────────── -->
  <groupId>io.decentralchain</groupId>
  <artifactId>wavesj</artifactId>
  <version>1.6.4.0</version>
  <packaging>jar</packaging>


  <!-- ── Project metadata (required for Maven Central) ───────────────── -->
  <name>wavesj</name>
  <description>Java library to interact with the DecentralChain blockchain (fork of wavesplatform/WavesJ)</description>
  <url>https://github.com/Decentral-America/DecentralChain</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <organization>
    <name>DecentralChain / Blockchain Costa Rica</name>
    <url>https://decentralchain.io</url>
  </organization>

  <developers>
    <developer>
      <id>decentralchain</id>
      <name>DecentralChain Team</name>
      <email>dev@decentralchain.io</email>
      <organization>DecentralChain</organization>
      <organizationUrl>https://decentralchain.io</organizationUrl>
      <roles>
        <role>Maintainer</role>
      </roles>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:git://github.com/Decentral-America/DecentralChain.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/Decentral-America/DecentralChain.git</developerConnection>
    <url>https://github.com/Decentral-America/DecentralChain/tree/dev/packages/jvm/wavesj</url>
  </scm>

  <issueManagement>
    <system>Jira</system>
    <url>https://decentralchain.atlassian.net/jira/software/projects/DCC/boards/1</url>
  </issueManagement>

  <!-- ── Properties ───────────────────────────────────────────────────── -->
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <!-- Reproducible builds: set to release date on each release. -->
    <project.build.outputTimestamp>2026-05-16T00:00:00Z</project.build.outputTimestamp>

    <!-- Plugin versions (all pinned to exact latest stable) -->
    <jacoco.version>0.8.14</jacoco.version>
    <spotbugs.version>4.9.8.3</spotbugs.version>

    <!-- JaCoCo coverage thresholds — unit-test-only baseline.
         Docker integration tests bring overall coverage higher.
         Override in CI: -Djacoco.line.ratio=0.50 -Djacoco.branch.ratio=0.40 -->
    <jacoco.line.ratio>0.20</jacoco.line.ratio>
    <jacoco.branch.ratio>0.07</jacoco.branch.ratio>
  </properties>

  <!-- ── Maven Central Portal ─────────────────────────────────────────── -->
  <repositories>
    <repository>
      <id>central-portal-snapshots</id>
      <url>https://central.sonatype.com/repository/maven-snapshots/</url>
      <releases><enabled>false</enabled></releases>
      <snapshots><enabled>true</enabled></snapshots>
    </repository>
  </repositories>

  <distributionManagement>
    <snapshotRepository>
      <id>central</id>
      <url>https://central.sonatype.com/repository/maven-snapshots/</url>
    </snapshotRepository>
  </distributionManagement>

  <!-- ── Build ────────────────────────────────────────────────────────── -->
  <build>
    <plugins>

      <!-- Enforcer: require Maven 3.9.2+ and Java 25 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.6.2</version>
        <executions>
          <execution>
            <id>enforce-versions</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireMavenVersion><version>[3.9.2,)</version></requireMavenVersion>
                <requireJavaVersion><version>[25,)</version></requireJavaVersion>
                <banDuplicatePomDependencyVersions/>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Compiler: Java 25 with all lints enabled -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <configuration>
          <release>25</release>
          <showWarnings>true</showWarnings>
          <showDeprecation>true</showDeprecation>
          <compilerArgs>
            <arg>-Xlint:all</arg>
            <arg>-Xlint:-serial</arg>
            <arg>-Xlint:-processing</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- Sources jar (required for Maven Central) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.4.0</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals><goal>jar-no-fork</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- Surefire: unit tests only (integration tests excluded by default) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.5</version>
        <configuration>
          <argLine>${jacoco.agent.argLine}</argLine>
          <!-- Exclude Docker-requiring integration tests from default verify -->
          <excludes>
            <exclude>**/*IntegrationTest.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- Javadoc jar (required for Maven Central) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.12.0</version>
        <configuration>
          <source>25</source>
          <doclint>none</doclint>
        </configuration>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals><goal>jar</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- JaCoCo: always-on coverage instrumentation -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
            <configuration>
              <propertyName>jacoco.agent.argLine</propertyName>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.line.ratio}</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.branch.ratio}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- SpotBugs: bytecode security analysis (always-on, runs during verify) -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>High</threshold>
          <failOnError>true</failOnError>
          <excludeFilterFile>config/spotbugs-exclude.xml</excludeFilterFile>
          <!-- FindSecBugs: OWASP security detector (1.14.0, Jun 2025) -->
          <plugins>
            <plugin>
              <groupId>com.h3xstream.findsecbugs</groupId>
              <artifactId>findsecbugs-plugin</artifactId>
              <version>1.14.0</version>
            </plugin>
          </plugins>
        </configuration>
        <executions>
          <execution>
            <id>spotbugs-check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!-- CycloneDX SBOM: supply-chain transparency -->
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>2.9.1</version>
        <executions>
          <execution>
            <id>make-bom</id>
            <phase>package</phase>
            <goals><goal>makeBom</goal></goals>
          </execution>
        </executions>
        <configuration>
          <!-- central-publishing-maven-plugin sets maven.deploy.skip=true which
               triggers CycloneDX's skipNotDeployed guard. Force-enable always. -->
          <skipNotDeployed>false</skipNotDeployed>
          <skip>false</skip>
          <projectType>library</projectType>
          <schemaVersion>1.6</schemaVersion>
          <includeBomSerialNumber>true</includeBomSerialNumber>
          <includeCompileScope>true</includeCompileScope>
          <includeTestScope>false</includeTestScope>
          <includeRuntimeScope>true</includeRuntimeScope>
          <includeSystemScope>false</includeSystemScope>
          <includeProvidedScope>false</includeProvidedScope>
          <outputName>bom</outputName>
        </configuration>
      </plugin>

    </plugins>
  </build>

  <!-- ── Dependency management (CVE overrides / version alignment) ─────── -->
  <dependencyManagement>
    <dependencies>
      <!-- commons-codec pulled transitively — pin to latest for CVE hygiene -->
      <dependency>
        <groupId>commons-codec</groupId>
        <artifactId>commons-codec</artifactId>
        <version>1.22.0</version>
      </dependency>
      <!-- commons-compress pulled transitively via protobuf / lang -->
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-compress</artifactId>
        <version>1.28.0</version>
      </dependency>
      <!-- protobuf pulled transitively via waves-transactions / lang -->
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>4.34.1</version>
      </dependency>
      <!-- jackson-databind aligned across all transitive consumers -->
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.21.3</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>2.21.3</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- ── Runtime Dependencies ─────────────────────────────────────────── -->
  <dependencies>

    <!--
      DCC fork of wavesplatform/waves-transactions (transaction serialization).
      Replaces upstream com.wavesplatform:waves-transactions:1.2.7.
      KNOWN-2 below tracks the remaining com.wavesplatform:lang dependency.
    -->
    <dependency>
      <groupId>io.decentralchain</groupId>
      <artifactId>transactions-java</artifactId>
      <version>1.0.0</version>
    </dependency>

    <!--
      KNOWN-2 (MEDIUM risk): Ride language compiler from Waves team.
      Will be replaced by io.decentralchain:lang_3 once DCC-252 publishes.
      See KNOWN_ISSUES.md for details.
    -->
    <dependency>
      <groupId>com.wavesplatform</groupId>
      <artifactId>lang</artifactId>
      <version>1.6.1</version>
    </dependency>

    <!-- ── Test dependencies ─────────────────────────────────────────── -->

    <!-- logback: runtime logging in tests only (not shipped to consumers) -->
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <!-- logback-classic 1.5.32 (Feb 2026): CVE-2026-1225 (ACE) fixed in 1.5.25 —
           never go below 1.5.25. -->
      <version>1.5.32</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.14.4</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <!-- assertj 3.27.7: latest STABLE — 4.0.0-M1 is PRE-RELEASE, do not use -->
      <version>3.27.7</version>
      <scope>test</scope>
    </dependency>

    <!--
      Testcontainers 2.x: junit-jupiter integration module was DROPPED in 2.x.
      Container lifecycle is managed via static initializers in
      BaseTestWithNodeInDocker. Integration tests excluded from surefire by
      default (see maven-surefire-plugin config above) — run via
      -P integration-test profile which requires Docker.
    -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
      <version>2.0.5</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>5.23.0</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <!-- ── Profiles ─────────────────────────────────────────────────────── -->
  <profiles>

    <!--
      audit: SpotBugs (bytecode) + PMD (source) + OWASP dependency check.
      Run with: ./mvnw verify -P audit [-Dnvd.api.key=<key>]
      NVD API key speeds up the dependency-check scan significantly.
      Without a key it still runs but is rate-limited.
    -->
    <profile>
      <id>audit</id>
      <build>
        <plugins>

          <!-- PMD: static source analysis (security + error-prone + best-practices) -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-pmd-plugin</artifactId>
            <version>3.28.0</version>
            <configuration>
              <rulesets>
                <ruleset>config/pmd-ruleset.xml</ruleset>
              </rulesets>
              <failOnViolation>true</failOnViolation>
              <printFailingErrors>true</printFailingErrors>
              <linkXRef>false</linkXRef>
            </configuration>
            <executions>
              <execution>
                <id>pmd-security-check</id>
                <phase>verify</phase>
                <goals><goal>check</goal></goals>
              </execution>
            </executions>
          </plugin>

          <!-- OWASP Dependency-Check: NVD CVE scan of all transitive deps -->
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <version>12.2.2</version>
            <configuration>
              <nvdApiKey>${nvd.api.key}</nvdApiKey>
              <failBuildOnCVSS>7</failBuildOnCVSS>
              <suppressionFiles>
                <suppressionFile>config/owasp-suppressions.xml</suppressionFile>
              </suppressionFiles>
            </configuration>
            <executions>
              <execution>
                <id>dependency-check</id>
                <phase>verify</phase>
                <goals><goal>check</goal></goals>
              </execution>
            </executions>
          </plugin>

        </plugins>
      </build>
    </profile>

    <!--
      integration-test: runs Docker-based integration tests.
      Requires Docker daemon + DCC node image available.
      Run with: ./mvnw verify -P integration-test
    -->
    <profile>
      <id>integration-test</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>3.5.5</version>
            <configuration>
              <argLine>${jacoco.agent.argLine}</argLine>
              <!-- Include ALL tests including integration tests -->
              <excludes/>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
      release: GPG signing + Maven Central Portal publish.
      Run with: ./mvnw deploy -P release (requires GPG key + CENTRAL_TOKEN env)
    -->
    <profile>
      <id>release</id>
      <build>
        <plugins>

          <!-- GPG sign all artifacts -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.8</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- Central Portal: publish to Maven Central (manual review gate) -->
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.10.0</version>
            <extensions>true</extensions>
            <configuration>
              <!-- autoPublish=false: human review required before release -->
              <autoPublish>false</autoPublish>
              <waitUntil>validated</waitUntil>
            </configuration>
          </plugin>

        </plugins>
      </build>
    </profile>

  </profiles>

</project>
