{
  "name": "transactions",
  "projectType": "library",
  "tags": ["scope:jvm", "layer:0", "lang:java"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./mvnw --batch-mode package -DskipTests",
        "cwd": "packages/jvm/transactions"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./mvnw --batch-mode clean",
        "cwd": "packages/jvm/transactions"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./mvnw --batch-mode test",
        "cwd": "packages/jvm/transactions"
      }
    },
    "verify": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./mvnw --batch-mode verify",
        "cwd": "packages/jvm/transactions"
      }
    }
  }
}
